{% import 'powerline.twig' as pwl %}

{% set segments = [
    {'content': battery|number_format, 'fg_color': fontColor, 'bg_color': batteryColor},
    {'content': wifi.essid, 'fg_color': fontColor, 'bg_color': wifiColor},
    {'content': sound.volume, 'fg_color': fontColor, 'bg_color': sound.isMuted ? soundMutedColor : soundUnmutedColor},
    {'content': time, 'fg_color': fontColor, 'bg_color': timeColor}
] %}

{{ pwl.render_segments(inject_splitters_gravity_right(segments, backgroundColor, splitterColor))}}
{{color_bg(backgroundColor)}}
