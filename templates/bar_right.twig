{% import 'macros.twig' as m %}
{{m.element('', (memory['available'] ?: 0)|binarySuffix, focusedDesktopColor)}}
  
{{m.element('', (filesystem ?: 0)|binarySuffix, occupiedDesktopColor)}}
  
{{m.element('', wifi.essid, wifiColor)}}
  
{{m.element( sound.isMuted ? '' : '', sound.volume~'%', soundUnmutedColor)}}
  
{{m.element('', battery|number_format~'%', soundMutedColor )}}
  
{{m.element('', time, timeColor)}}
 
