{% import 'macros.twig' as m %}
{{m.element('', sysload.current)}} {{sparkline(sysload.history)}}
  
{{m.element('', (memory['available'] ?: 0)|binarySuffix)}}
  
{{m.element('', (filesystem ?: 0)|binarySuffix)}}
  
{{m.element('', wifi.essid)}}
  
{{m.element( sound.isMuted ? '' : '', sound.volume~'%')}}
  
{{m.element('', battery|number_format~'%')}}
  
{{m.element('', time)}}
 
