{% import 'powerline.twig' as pwl %}

{% set background = '#FF20201d' %}
{% set green = '#FF60ac39' %}
{% set foreground = '#FFfefbec' %}

{{pwl.new_segment_rtl(green, background, battery|number_format)}}
  {{ wifi.essid }}
  {{ sound.volume }}
  {{ time }}
 
{{color_bg(background)}}
